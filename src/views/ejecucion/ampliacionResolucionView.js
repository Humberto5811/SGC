/**
 * Ampliaciones / Resolución — RC8.6B
 * NO IMPLEMENTADO: stub sin bandeja funcional.
 * Al desarrollarse debe usar catálogo central y EstadoResponsableCell
 * con estados reales del flujo (si existen): SOLICITADA, EN_REVISION, OBSERVADA, etc.
 */
function renderAmpliacionResolucionView() {
  return `<h1 class="h3">Ampliación Resolución</h1>
    <p class="text-muted mb-2">Vista en construcción.</p>
    <p class="small text-muted mb-0"><strong>NO IMPLEMENTADO</strong> — sin bandeja funcional ni estados inventados.
    Al implementarse, usar <code>estadoCatalogo</code> y componentes centrales de <code>src/ui/workflow/</code>.</p>`;
}
function initAmpliacionResolucionView() { console.log('Ampliación Resolución — stub NO IMPLEMENTADO'); }
export { renderAmpliacionResolucionView, initAmpliacionResolucionView };
